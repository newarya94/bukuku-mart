import 'package:flutter/material.dart';

class SalesTransactionView extends StatelessWidget {
  const SalesTransactionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
